# Owlett 中文使用说明

[简体中文](USER_GUIDE.md) | [English](USER_GUIDE.en.md)

Owlett 帮你录下鸟声、回听识别片段、准备观鸟计划，并与 AI 助手讨论鸟类知识。
实时识别在手机上进行；联网功能需要你自己的服务配置。识别与 AI 建议都有可能出错，请结合实际观察判断。

## 1. 安装
下载状态见 [安装与配置 API](README.md#安装与配置-api)。正式版名称 Owlett，应用编号 io.github.greatwyj1.owlett。安装提供的正式 APK，系统询问时允许当前文件管理器安装应用。
首次进入录音、计划、助手页面会显示简短教程，可跳过；之后在“设置 → 使用帮助”重看。
进入页面不会立即申请麦克风权限。开始录音时才申请。

## 2. 从旧测试版迁移
旧版与正式版是两个不同应用，可以同时安装。不要卸载旧版。
1. 使用“旧版迁移 APK”更新旧测试版。必须先核对已安装应用与迁移 APK 签名一致；出现“签名不一致”就停止，不能通过卸载解决。
2. 在旧版停止录音并等待所有识别、助手和鸟况整理结束，打开“设置 → 存储管理 → 导出数据”，选择本机存放位置。
3. 安装正式版，尚未创建计划、录音或聊天时，进入同一位置点击“导入数据”，选择刚才的 ZIP。
4. 等待校验完成，点击“重新打开应用”。检查计划、关联、录音、聊天都在，再重新填写各服务密钥。
5. 核对迁移成功前保留旧版和备份。备份不设密码，含私人录音、地点与聊天，不要上传到公开位置。

本版仅支持迁入空白正式版，不能合并两套已有数据。至少预留解压数据两倍加 64 MB 的空间。
临时文件、模型、图鉴、网络缓存和服务 Key 不随迁移；未完成或待确认的技能需要重新尝试，不会导入后自动执行。

## 3. 录音与回顾
在“录音”页点击开始。实时识别记录自动保存至本机录音行程。可暂停、恢复，结束后在“计划与行程 → 行程”列表查看。
暂停后频谱默认“滑动”：拖动查看时间轴。右上角可切换“时间选区”或“频谱选区”。
单击设红色播放起点；拖出时间窗后播放选中片段。频谱框选会带通滤波，仅保留选定频率附近的声音。
回顾页与录音页使用同样操作。删除音频后仍可保留识别记录；删除整个行程会同时移除计划关联。

## 4. 精准识别：需要自部署
**目前不提供公共精准识别后端。需要此功能时请自行配置服务。**
部署步骤在 server/precise_recognition/DEPLOYMENT.md。没有服务器或不会部署，可继续使用手机实时识别。
“设置 → 精准识别”填写自己的服务地址和访问令牌（Token），选择 BirdNET 2.4 或 Perch v2。
选区提交时会说明发送目标，发送音频选区、时间和识别参数；启用定位时也发送坐标。
手机访问服务时不能填手机自己的 127.0.0.1。局域网使用服务器的局域网地址；远程建议 HTTPS。
频谱选区提交的是带通滤波后的音频。不会因此上传整个录音行程。

## 5. 观察结果说明

结果按完整鸟点名列出每份报告的观测时间或时段、该鸟数量及“原报告”入口；记录多时点击“更多记录”或翻页，也可让助手使用刚才的缓存继续整理。时间指报告观测时段，不保证是目标鸟的精确出现时刻；数量不明确时显示“未注明”。不同报告可能见到同一个体，因此不能相加当作鸟的总数。该查询只回答在哪里、何时有记录，不能从这些已筛选报告计算当地出现频率或稀有程度。

## 6. Owlett 助手
“设置 → DeepSeek”保存你自己的 Key，测试连通性并选择模型。测试只查询模型列表，不生成回答。
消息、近期对话和所选附件摘要会发往 DeepSeek；不要把密码或 Key 当消息发给助手。
可创建多个会话、重命名、停止回答和重试。加号每条消息最多添加一个计划或已完成行程。
在输入框输入 / 选择技能，也可直接提需求：
- /plan 创建计划；/activity 整理鸟况；/targets 调整目标。
- /bird 查鸟种资料；/calls 查询并播放 xeno-canto 鸣声。
- /link 把完成的录音行程关联到计划，例如“把 9.6 上午的录音关联到当天北京大学的计划”。匹配不唯一会请你选择。
- /clips 按已保存识别记录整理片段，例如“整理这个行程里大杜鹃置信度大于 30% 的叫声”。只看音频置信度，不重新识别、不上传录音。
“大于 30%”不包含恰好 30%；“不低于 30%”包含。不要写“可信度大于 3”等含糊阈值。
片段列表可播放、暂停、拖动进度、上一段/下一段、连续播放和查看原频谱。不会自动出声；开始录音会停止播放。

“设置 → 助手行为”可修改初始提示词（最多 10,000 字符），保存或恢复默认。
“自动使用技能”控制模型能否主动选择技能；“操作确认方式”控制写数据前是否询问，两者独立。
默认询问；“全部自动”只开放已有写技能，信息不全仍追问。不支持删除行程、任意文件访问或修改密钥。
设置影响新请求，重试沿用原回复的提示词和确认方式；不会自动执行旧的待确认卡。

## 7. 鸣声与分享
鸣声查询是可选功能，需要在“设置 → xeno-canto”填写自己的 API v3 Key。

鸟鸣默认先查系统位置对应的地区，再查所在国家；只有该国没有该鸟种记录时才扩大到全球，并说明原因。对话或附件计划涉及其他地区时优先使用该地区；明确指定地区查不到时先询问，不擅自扩大。没有可用位置时请告诉助手国家/地区，或在系统及应用设置中开启位置功能。

点击鸟鸣播放后，等待期间显示加载进度；开始播放后仍会按需缓存已加载部分。完整播过的录音可从缓存重播，没加载过的部分仍需联网。缓存最多256 MB，过满自动清理旧内容；也可在“设置 → xeno-canto → 清空鸟鸣音频缓存”主动清空，清理会停止当前播放，不删除自己的录音行程。

## 8. 常见问题
| 问题 | 处理方式 |
| --- | --- |
| Key 无效、401/403 | 确认 Key 属于对应服务，重新填写；不要把 eBird Key 填到 DeepSeek。 |
| 429、请求太多 | 等一会再试，检查额度；反复点击会更容易限流。 |
| 连接超时 | 检查网络、服务地址、端口、防火墙；真实手机不能用模拟器专用地址。 |
| 未找到鸟点 | 换地区或更短的关键词；这不等同于网络失败。 |
| 无报告、覆盖率不足 | 日期或鸟点可能没有数据；旧分析会保留，不把失败当作“没有鸟”。 |
| 模型未加载 | 检查资源包是否齐全并匹配；源码构建先执行资源准备脚本。 |
| 音频缺失 | 可能已清理原录音；聊天片段引用不等于音频备份。 |
| 无法录音 | 在系统应用权限中允许麦克风，并检查其他应用是否占用麦克风。 |
| 安装签名不一致 | 不要卸载旧版；找到正确签名的更新包后迁移。 |
| 导入空间不足 | 清理手机空间，预留至少迁移解压大小的两倍。 |
| 导入中断 | 重新启动会撤销未完成导入；检查空白状态后重新选择原包。 |
| AI 回答不准确 | 查看引用的数据日期与技能结果；改用明确鸟种、日期、阈值。 |

## 9. 数据与许可
录音、计划、聊天保存在手机私有目录。服务密钥不会随迁移导出；你自己在聊天里输入的秘密不属于“设置密钥”，不会被自动识别删除。
删除或分享副本后，外部应用的留存由其管理。完整说明见 PRIVACY.md。
自研代码为 PolyForm Noncommercial 1.0.0，允许非商业用途下修改和分发并保留来源。组件、模型、图片及鸣声依照自身许可，参见“设置 → 关于 → 第三方许可与数据来源”。
## 助手场景技能与回收站

输入 `/` 可以选择场景技能。除原有功能外，`/settle` 用于结算，`/settings` 用于普通设置。助手也能根据自然语言组合操作，例如“把这两个计划改到下周，再关联今天的录音”。对象不明确时需要先选择。

默认修改前展示预览。自动执行不会开放密钥、服务地址、删除录音等权限。中途失败时已完成的修改不会自动撤销，请查看每一步结果；同一操作重试有防重复保护。关闭自动技能后，手动选择的场景只开放相关工具。

计划列表右上角可进入回收站。删除计划只移入回收站，30天内能恢复内容与关联；到期清理计划不影响录音。升级或进程中断后的未完成操作需要重试，不会自行继续修改数据。
## 1.0.1 新增入口

- **更换配色**：设置 → 外观 → 主题配色，可选清爽绿、羽色·灰褐、羽色·暖金；下方明暗模式仍可选跟随系统、浅色、深色。
- **分享选区**：暂停或回顾录音，选中时间窗或频谱区域，点击精准识别右侧的分享图标。没有有效选区、音频被删除、正在滤波或正在导出时按钮不可用；原来的长按分享仍可使用。
- **快速精准识别**：录音底部“更多录音操作”菜单中保留快捷入口。
- **鸟种小图**：识别、计划清单与结算条目左侧显示本地图鉴图片；点击图片查看资料，不会改变清单勾选。缺少图片时显示占位图。
- **核对版本**：设置 → 关于 Owlett 显示版本及内部版本号；“更新记录”可查看每轮变化和未验收项。相同签名的内测更新包覆盖安装即可，不要先卸载。
